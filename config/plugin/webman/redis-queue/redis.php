<?php
return [
    'default' => [
        'host' => sprintf(
            'redis://%s:%s',
            env('REDIS_HOST', '127.0.0.1'),
            env('REDIS_PORT', 6379)
        ),
        'options' => [
            'auth' => env('REDIS_PASSWORD', null),       // 密码，字符串类型，可选参数
            'db' => env('REDIS_DB', 0),                  // 数据库
            'prefix' => '',                              // key 前缀

            // ============================================================
            // 重试策略（针对游戏记录等关键业务）
            // ============================================================
            // max_attempts: 最大重试次数（总共尝试 1+3=4 次）
            //   - 第1次：立即处理（消费失败）
            //   - 第2次：3秒后重试
            //   - 第3次：6秒后重试
            //   - 第4次：9秒后重试（最后一次）
            //
            // retry_seconds: 重试间隔（每次重试间隔3秒）
            //   - 更快的重试适合游戏记录（数据重要且时效性高）
            //   - 避免短暂的网络抖动或数据库慢查询导致数据丢失
            //
            // 总重试时间：约 18 秒（3+6+9）
            // ============================================================
            'max_attempts' => 3,                         // 重试3次（共4次尝试）
            'retry_seconds' => 3,                        // 重试间隔3秒（更快）
        ]
    ],
];
